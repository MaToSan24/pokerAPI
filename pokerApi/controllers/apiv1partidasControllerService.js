'use strict'

module.exports.getPartidas = function getPartidas(req, res, next) {
  res.send({
    message: 'This is the mockup controller for getPartidas'
  });
};

module.exports.addPartida = function addPartida(req, res, next) {
  res.send({
    message: 'This is the mockup controller for addPartida'
  });
};